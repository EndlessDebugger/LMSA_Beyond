<%= form_with(model: point) do |form| %>
  <% if point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(point.errors.count, "error") %> prohibited this point from being saved:</h2>

      <ul>
        <% point.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :referrals %>
    <%= form.number_field :referrals %>
  </div>

  <div class="field">
    <%= form.label :prof_referrals %>
    <%= form.number_field :prof_referrals %>
  </div>

  <div class="field">
    <%= form.label :gen_event %>
    <%= form.number_field :gen_event %>
  </div>

  <div class="field">
    <%= form.label :fund_event %>
    <%= form.number_field :fund_event %>
  </div>

  <div class="field">
    <%= form.label :vol_event %>
    <%= form.text_field :vol_event %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
