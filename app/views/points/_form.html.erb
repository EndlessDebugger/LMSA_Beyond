<%= form_with(model: point) do |form| %>
  <% if point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(point.errors.count, "error") %> prohibited this point from being saved:</h2>

      <ul>
        <% point.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.number_field :name %>
  </div>

  <div class="field">
    <%= form.label "Point Value" %>
    <%= form.number_field :val %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.number_field :desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
