require 'rails_helper'

RSpec.describe "AwardPoints", type: :request do
  describe "GET /index" do
    it "returns http success" do
      get "/award_points/index"
      expect(response).to have_http_status(:success)
    end
  end

end
